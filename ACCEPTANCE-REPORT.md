# NC25OL Acceptance Report

Version: `0.0.11`

Verification date: `2026-09-24 UTC`

Status: `PASS`

## Executable acceptance

Run from the package root:

```text
python -B tests/run_acceptance.py
```

Required terminal result:

```text
ACCEPTANCE_STEPS=8/8 PASS
```

| Gate | Recorded result |
|---|---:|
| Functional behavior across all three reference profiles | `93/93 PASS` |
| Semantic contracts | `15/15 PASS` |
| Named safety mutations | `51/51 PASS` |
| JSON Schema and OpenAPI conformance | `55/55 PASS` |
| Contract regression cases | `89/89 PASS` |
| OTCS bridge behaviour and refusal vocabulary | `67/67 PASS` |
| Failure-surface ratchet | `1/1 PASS` |
| Package integrity checks | `66/66 PASS` |

The contract regression cases cover hash-case normalization, executor route/body
permit binding, malformed evidence and execution-request refusal without
state or caller-input changes, disposition/message-code binding, early and late commit fault
rollback, authority-before-resolver precedence, submitted-request idempotency
across resolver retries, declaration-bound action-target pairs, fixed banking
failure posture, profile/declaration identifier-list validation, SQLite restart
recovery, current-snapshot public reads, and authenticated malformed-state
refusal across reads, evaluation and reopen,
single-debit concurrent consumption, SQLite persist-failure rollback, observer
message normalization, selector-bound registry lookup, exact stored-receipt
replay after declaration expiry, mapping-witness hash case normalization, DOCX
audit-projection currency, source-bundle determinism and manifest
binding,
acceptance-report machine-field discipline, DOCX property hygiene,
public-report hygiene, detached Ed25519 tamper/wrong-key rejection,
authoritative resolver override, declared capacity provenance typing across
the schema and engine surfaces, hostile values across the wire, the persisted
state row and the stored chain answering inside the refusal vocabulary, every
refusal of the acceptance-report generator by name, and
resolver exception or malformed-output
fail-closed behavior.

## Bound bytes

This report was generated for one exact tree. The manifest entries it
certifies, every entry except this report, digest to `4AE4D978D4603557F75E63494B65B21A0D034738A237ED638B610FB62F6B7896`
as path-ordered (path, SHA-256, byte length) triples; the specification
document the geometry below describes has SHA-256 `FE6D25D0253E9F6F952E696440531CA838866D8B26B9FAF52F60E6FEC7A5D840`.
The package verifier recomputes both from the shipped bytes and refuses
this report when either differs.

## Core source binding

The immutable NC2.5 core `NC25_v3_0_patched_v4.tex` was bound and verified
during this acceptance run: SHA-256 `20F1EA17E0B986627CAD9AFA7C02E1DC5088C43A779435E5A38F11E43A06C9C3`, 1414184 bytes,
14228 lines, byte- and line-exact (source mode `strict`). A
release-grade acceptance report is refused unless this external-core
verification passed.

The core is not shipped with this package, so a run without it reports
`PACKAGE_CHECKS=61/61` and `EXTERNAL_SOURCE_CHECKS=NOT_RUN` while the
overall verdict stays `ACCEPTANCE_STEPS=8/8 PASS`. The package-integrity figure
recorded in the table above is the release configuration, with the bound
local core file supplied through `NC25_SOURCES_ROOT`. This revision
is not part of the public deposit; the DOI identifies the core line,
not the availability of this exact file.

## Failure-surface figures

The failure-surface step measured, for the exact bytes this report
certifies: 296 engine refusal codes and 16 wire error codes in the
closed world; raise and helper-emission points all=531, static=519, dynamic=12;
296 engine refusal codes constructed and 296 witnessed during the
recorded run; 0 codes unwitnessed; 1 code re-emitted by the
translation site belonging to neither vocabulary; and 73 OTCS bridge refusal codes in a closed world of their own. Each figure is a property
of the named procedure recorded in the baseline, not of the engine in the
abstract, and the committed baseline is a no-growth debt ratchet, not a
completeness claim.

## Document artifact

`NC25OL Specification v0.0.docx` is generated by the
shipped deterministic builder. Its headless LibreOffice PDF render contains
22 pages and uses 1 page geometry. The machine-readable
geometry gate reported `GREEN`, 0 hard findings, and
0 non-blocking warnings. Automated geometry evidence does not claim
human visual inspection. Optional extended-property counters that could not be
derived truthfully were removed from the DOCX package.

## Security and data boundary

The package contains synthetic examples only. It contains no reusable
credentials, raw customer evidence, personal data, or production secrets.
Registry examples remain hash-only. Operator outputs remain normalized and do
not expose gate geometry, thresholds, margins, or detailed reason codes.

## Production boundary

This is a reference and conformance package, not a production deployment.
Production requires enterprise workload identity, mTLS and scoped OAuth,
KMS/HSM-backed asymmetric signatures (the reference Ed25519 event signer does
not provide managed custody), trusted time, downstream action/receipt
atomicity, managed or replicated state where multi-node availability is required,
durable cross-system reservation coordination, append-only or WORM evidence
storage, and independent deployment assurance.

This report is a conformance record at an observation point. It certifies the
exact bytes for which it was generated, at the date recorded above; it does
not reconstruct what happened between verification runs, and it does not
measure remaining continuation capacity. Those are different questions, and
they require different instruments.
